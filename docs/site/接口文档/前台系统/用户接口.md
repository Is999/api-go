# 用户接口

## 当前用户资料

`GET /api/user/profile`

### 请求头

| 字段 | 类型 | 是否必须 | 默认值 | 字段解释 |
|------|------|----------|--------|----------|
| Authorization | string | 是 |  | `Bearer <token>` |

### 安全字段

| 项目 | 字段 |
|------|------|
| 请求签名字段 | 不参与签名 |
| 请求加密字段 | 不参与加密 |
| 响应签名字段 | 不参与签名 |
| 响应加密字段 | email, phone |

### 响应示例

```json
{
  "status": true,
  "code": 1110,
  "message": "获取成功",
  "data": {
    "id": 778919762005200896,
    "shardNo": 896,
    "username": "demo_user",
    "nickname": "Demo",
    "email": "demo@example.com",
    "phone": "",
    "avatar": "",
    "status": 1,
    "lastLoginAt": "2026-06-02 12:00:00",
    "lastLoginIp": "127.0.0.1",
    "createdAt": "2026-06-02 12:00:00",
    "updatedAt": "2026-06-02 12:00:00"
  }
}
```

### data 内部字段

| 字段 | 类型 | 是否必须 | 默认值 | 字段解释 |
|------|------|----------|--------|----------|
| id | int | 是 |  | 用户 ID |
| shardNo | int | 是 |  | 用户取模分片，等于 `id % 1000` |
| username | string | 是 |  | 用户名 |
| nickname | string | 是 |  | 昵称 |
| email | string | 是 |  | 邮箱 |
| phone | string | 是 |  | 手机号 |
| avatar | string | 是 |  | 头像 |
| status | int | 是 |  | 状态：1 正常，0 禁用 |
| lastLoginAt | string | 否 |  | 最后登录时间 |
| lastLoginIp | string | 否 |  | 最后登录 IP |
| createdAt | string | 是 |  | 创建时间 |
| updatedAt | string | 是 |  | 更新时间 |

## 内网同步用户运行态

`POST /internal/users/:id/runtime-sync`

该接口为内网运维路由，不允许公网访问；仅用于 admin 后台直接修改 `user` 表后，通知 API 进程失效用户资料缓存或登录态。用户表增删改仍由各自服务或后台 DB 管理，不通过该接口代理。

### 请求头

| 字段 | 类型 | 是否必须 | 默认值 | 字段解释 |
|------|------|----------|--------|----------|
| X-Ops-Token | string | 是 |  | 运维接口令牌，需匹配 `ops.config_reload_token` |

### 请求体

| 字段 | 类型 | 是否必须 | 默认值 | 字段解释 |
|------|------|----------|--------|----------|
| profile | bool | 否 | true | 是否失效用户资料缓存；`profile` 和 `sessions` 都为 false 时默认按 true 处理 |
| sessions | bool | 否 | false | 是否失效该用户全部登录态 |
| reason | string | 否 |  | 同步原因，仅用于日志排查 |

### 安全字段

| 项目 | 字段 |
|------|------|
| 请求签名字段 | 不参与签名 |
| 请求加密字段 | 不参与加密 |
| 响应签名字段 | 不参与签名 |
| 响应加密字段 | 不参与加密 |

### 响应示例

```json
{
  "status": true,
  "code": 1106,
  "message": "更新成功",
  "data": {
    "userId": 10001,
    "profileCacheInvalidated": true,
    "sessionsInvalidated": true,
    "reason": "admin_reset_user_password"
  }
}
```

### data 字段说明

| 字段 | 类型 | 是否必须 | 默认值 | 字段解释 |
|------|------|----------|--------|----------|
| userId | int | 是 |  | 用户 ID |
| profileCacheInvalidated | bool | 是 | false | 是否已处理用户资料缓存 |
| sessionsInvalidated | bool | 是 | false | 是否已处理登录态 |
| reason | string | 否 |  | 本次同步原因 |
